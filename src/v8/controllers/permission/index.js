const find = require('./find')
const create = require('./create')
const remove = require('./remove')
const findAll = require('./findAll')
const create_bulk = require('./create-bulk')

module.exports = {
    find,
    create,
    remove,
    findAll,
    create_bulk
}
